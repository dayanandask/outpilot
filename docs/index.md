# Outreach Pipeline

Automated cold-outreach pipeline — production-ready, secure, scalable, well-tested, and documented.

## Quickstart

```bash
uv sync
cp .env.example .env
uv run python -m pipeline run acme.com
```

## Prerequisites

- Python 3.12+
- `uv` package manager
